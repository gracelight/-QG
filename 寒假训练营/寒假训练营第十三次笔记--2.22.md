#实践任务 
1.LED流水灯（定时器触发）
2.0-99计数



#问题解决 
实际配置定时器使用stc自带的生成代码再加上
ET0(零)=1;     //允许中断
EA=1;       //允许总中断
PT0（零）=0;    //中断为低优先级
删去这个
AUXR &= 0x7F;		//没有定义
还要赋初值
TL0=（0x66;）	
TH0=（0xFC;） //我是基于11.0592MHz晶振进行计算
可以直接模块化处理，但不是常规思路引用，而是借用

江协科技使用			if(LEDMode==1)P2=_cror_(P2,1);
			     if(LEDMode==0)P2=_crol_(P2,1);
 实现灯左右移动不过要加上#include <INTRINS.H>头文件